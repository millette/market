'use strict'

// WIP

export default {
  state: [
    {
      name: 'Jean Crest',
      classes: [0]
    }
  ],
  reducers: {
    // login: (state, payload) => ({ ...payload, loginAt: Date.now() }),
  },
  effects: {
    /*
    loginAsync (name, rootState) {
      return this.login({ name })
    }
    */
  }
}
